---
layout: base.njk
title: "Events - Christo's Prints"
description: "Find Christo's Prints at upcoming events and discover limited edition seasonal prints."
activePage: events
permalink: /events/
---

<!-- Page Header -->
<div class="page-header">
  <h1>Events</h1>
  <p>Find us in person and check out seasonal specials</p>
</div>

{%- if events.upcoming | length > 0 %}
<!-- Upcoming Events -->
<section class="section" style="padding-top: 0;">
  <div class="section-header">
    <h2>Upcoming Events</h2>
    <p>Come see us and shop in person</p>
  </div>
  <div class="features-grid">
    {%- for event in events.upcoming %}
    <div class="feature-card event-card" style="text-align: left;">
      <div class="event-date">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
        {{ event.date }}
      </div>
      <h3>{{ event.name }}</h3>
      <p>{{ event.description }}</p>
      {%- if event.location %}
      <div class="event-location">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg>
        {{ event.location }}
      </div>
      {%- endif %}
    </div>
    {%- endfor %}
  </div>
</section>
{%- endif %}

{%- if events.seasonal | length > 0 %}
<!-- Seasonal Specials -->
<section class="section" style="padding-top: 0;">
  <div class="section-header">
    <h2>Seasonal Specials</h2>
    <p>Limited edition prints for holidays and special occasions</p>
  </div>
  <div class="seasonal-groups">
    {%- for group in events.seasonal | groupByBadge %}
    <div class="seasonal-group" id="seasonal-{{ loop.index }}">
      <button class="seasonal-group-header" onclick="toggleSeasonalGroup('seasonal-{{ loop.index }}')" aria-expanded="false">
        <div class="seasonal-group-info">
          <h3>{{ group.badge }} Collection</h3>
          <span class="seasonal-group-count">{{ group.items | length }} item{{ "s" if group.items | length != 1 }}</span>
        </div>
        <svg class="seasonal-group-chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
      </button>
      <div class="seasonal-group-items">
        <div class="seasonal-group-items-inner">
          <div class="product-grid">
            {%- for item in group.items %}
            <div class="product-card">
              {%- if item.image %}
              <div class="product-card-img">
                <img src="/{{ item.image }}" alt="{{ item.name }}">
                {%- if item.badge %}
                <span class="product-card-badge">{{ item.badge }}</span>
                {%- endif %}
              </div>
              {%- endif %}
              <div class="product-card-body">
                <h3>{{ item.name }}</h3>
                <p class="description">{{ item.description }}</p>
                <div class="product-card-footer">
                  <span class="price">{{ item.price }}</span>
                  <button class="btn btn-primary btn-small btn-add-to-cart"
                    data-name="{{ item.name }}" data-price="{{ item.price }}" data-type="product"
                    onclick="Cart.addItem(this.dataset.name, this.dataset.price, this.dataset.type); Cart.flashAdded(this);"
                    aria-label="Add {{ item.name }} to basket">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/><line x1="12" y1="3" x2="12" y2="9"/><line x1="9" y1="6" x2="15" y2="6"/></svg>
                  </button>
                </div>
              </div>
            </div>
            {%- endfor %}
          </div>
        </div>
      </div>
    </div>
    {%- endfor %}
  </div>
</section>

<script>
function toggleSeasonalGroup(id) {
  var group = document.getElementById(id);
  var btn = group.querySelector('.seasonal-group-header');
  var items = group.querySelector('.seasonal-group-items');
  var inner = group.querySelector('.seasonal-group-items-inner');
  var expanded = btn.getAttribute('aria-expanded') === 'true';

  if (expanded) {
    items.style.height = items.scrollHeight + 'px';
    requestAnimationFrame(function() {
      items.style.height = '0px';
    });
    btn.setAttribute('aria-expanded', 'false');
    group.classList.remove('expanded');
  } else {
    items.style.height = inner.offsetHeight + 'px';
    btn.setAttribute('aria-expanded', 'true');
    group.classList.add('expanded');
    items.addEventListener('transitionend', function handler() {
      if (btn.getAttribute('aria-expanded') === 'true') {
        items.style.height = 'auto';
      }
      items.removeEventListener('transitionend', handler);
    });
  }
}
</script>
{%- endif %}

{%- if events.upcoming | length == 0 and events.seasonal | length == 0 %}
<!-- Nothing Right Now -->
<section class="section" style="padding-top: 0;">
  <div class="cart-empty">
    <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="var(--text-muted)" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"/><line x1="16" y1="2" x2="16" y2="6"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="3" y1="10" x2="21" y2="10"/></svg>
    <h2>Nothing coming up right now</h2>
    <p>Check back soon for upcoming events and seasonal limited editions.</p>
    <a href="/products/" class="btn btn-primary" style="margin-top: 16px;">Browse Products</a>
  </div>
</section>
{%- endif %}
